# =============================================================================
# Flox SCA Demo - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your credentials:
#   cp .env.example .env
#
# Docker Compose automatically loads .env from the project root.
# =============================================================================

# -----------------------------------------------------------------------------
# NVD (National Vulnerability Database)
# -----------------------------------------------------------------------------
# Get a free API key at: https://nvd.nist.gov/developers/request-an-api-key
# Without key: 5 requests per 30 seconds
# With key: 50 requests per 30 seconds (10x faster)
NVD_API_KEY=

# -----------------------------------------------------------------------------
# Snyk
# -----------------------------------------------------------------------------
# Get your API token from: https://app.snyk.io/account
# Get your Org ID from: https://app.snyk.io/org/<your-org>/manage/settings
SNYK_TOKEN=
SNYK_ORG_ID=

# -----------------------------------------------------------------------------
# Black Duck (coming soon)
# -----------------------------------------------------------------------------
# URL: Your Black Duck Hub instance URL (e.g., https://blackduck.company.com)
# Token: Generate from Black Duck Hub > Profile > Access Tokens
BD_URL=
BD_TOKEN=

# -----------------------------------------------------------------------------
# SonarQube
# -----------------------------------------------------------------------------
# URL: Your SonarQube server URL (e.g., https://sonarqube.company.com)
# Token: Generate from SonarQube > My Account > Security > Tokens
#        Token format: squ_xxxxxxxxxxxxx (User Token for Bearer auth)
# Note: Projects must be scanned with sonar-scanner first. This integration
#       queries existing analysis results - it does not submit SBOMs directly.
SONAR_URL=
SONAR_TOKEN=

# -----------------------------------------------------------------------------
# Sonatype Nexus IQ (coming soon)
# -----------------------------------------------------------------------------
# URL: Your Nexus IQ Server URL (e.g., https://iq.company.com)
# Token: Generate from Nexus IQ > User Settings > User Token
# App ID: Application identifier for scans (default: flox-sca)
IQ_URL=
IQ_TOKEN=
IQ_APP_ID=flox-sca

# -----------------------------------------------------------------------------
# FOSSA (License Compliance)
# -----------------------------------------------------------------------------
# Get your API token from: https://app.fossa.com > Settings > API Tokens
# FOSSA provides license compliance scanning and vulnerability detection
FOSSA_TOKEN=

# -----------------------------------------------------------------------------
# DefectDojo (Vulnerability Management)
# -----------------------------------------------------------------------------
# URL: Your DefectDojo instance URL (default: http://localhost:8443 for local Docker)
# Token: Generate from DefectDojo > API v2 > Tokens (or Admin > API Key)
# DefectDojo provides remediation workflows, SLA tracking, and JIRA integration.
# To run DefectDojo locally: docker-compose -f docker-compose.defectdojo.yml up -d
DEFECTDOJO_URL=http://localhost:8443
DEFECTDOJO_API_TOKEN=

# -----------------------------------------------------------------------------
# PostgreSQL (internal - usually no need to change)
# -----------------------------------------------------------------------------
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=sca_demo
POSTGRES_USER=sca
POSTGRES_PASSWORD=sca_password

# -----------------------------------------------------------------------------
# Splunk (internal - usually no need to change)
# -----------------------------------------------------------------------------
# Only needed if pointing to external Splunk instance
# SPLUNK_HEC_URL=https://splunk:8088
# SPLUNK_HEC_TOKEN=sca-demo-token
# SPLUNK_INDEX=sca_vulnerabilities

# -----------------------------------------------------------------------------
# Grafana (internal - usually no need to change)
# -----------------------------------------------------------------------------
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=admin
